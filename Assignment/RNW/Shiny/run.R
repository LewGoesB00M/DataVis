# This file isn't mandatory, I think runApp is actually meant to be called from the console.
library(shiny)

# THIS DOES NOT RUN IN DATASPELL.
# FOR SOME REASON, IT CAN'T RUN SHINY APPS.

# When using directories in R, use forward slashes rather than backslashes because the latter
# are treated as escape characters and will cause errors.
runApp("C:/Users/Lewis/DataspellProjects/DataVis/Labs/Lab 9")